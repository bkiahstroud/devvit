<%= form_with(model: subdevvit, local: true) do |form| %>
  <% if subdevvit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subdevvit.errors.count, "error") %> prohibited this subdevvit from being saved:</h2>

      <ul>
        <% subdevvit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
